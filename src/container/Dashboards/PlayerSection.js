import React from "react";

const PlayerSection = ({ player }) => {
  console.log(player);

  return (
    <div>
      {/* {player.map((p) => {
        return <h1 key={p}>{p}</h1>;
      })} */}
    </div>
  );
};

export default PlayerSection;
